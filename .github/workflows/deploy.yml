# name: Build and Deploy frontend-service

# on:
#   push:
#     branches: [main]

# permissions:
#   id-token: write
#   contents: read

# env:
#   AWS_REGION: us-east-1
#   ECR_REPOSITORY: acme-cloud-poc-frontend
#   EKS_CLUSTER_NAME: acme-cloud-poc-eks

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       # Same trusted OIDC role from Phase 5 — its trust policy already
#       # includes frontend-service in the allowed repo list.
#       - name: Configure AWS credentials via OIDC
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: arn:aws:iam::338449997393:role/acme-cloud-poc-github-deploy-role
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Login to Amazon ECR
#         id: ecr-login
#         uses: aws-actions/amazon-ecr-login@v2

#       - name: Build and push image
#         env:
#           ECR_REGISTRY: ${{ steps.ecr-login.outputs.registry }}
#           IMAGE_TAG: ${{ github.sha }}
#         run: |
#           docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
#           docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
#           echo "IMAGE_URI=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_ENV

#       - name: Update kubeconfig
#         run: aws eks update-kubeconfig --name ${{ env.EKS_CLUSTER_NAME }} --region ${{ env.AWS_REGION }}

#       - name: Deploy to EKS
#         run: |
#           sed -i "s|IMAGE_PLACEHOLDER|${IMAGE_URI}|g" k8s/deployment.yaml
#           kubectl apply -f k8s/deployment.yaml
#           kubectl apply -f k8s/service.yaml
#           kubectl apply -f k8s/ingress.yaml
#           kubectl rollout status deployment/frontend-service --timeout=180s
