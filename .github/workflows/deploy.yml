# name: Build and Deploy frontend-service

# on:
#   push:
#     branches: [main, dev, qa]

# permissions:
#   id-token: write
#   contents: read

# env:
#   AWS_REGION: us-east-1

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Resolve environment
#         id: resolve
#         run: |
#           case "${{ github.ref_name }}" in
#             main) ENV=poc ;;
#             dev)  ENV=dev ;;
#             qa)   ENV=qa ;;
#             *) echo "Unrecognized branch '${{ github.ref_name }}'" && exit 1 ;;
#           esac
#           echo "env=${ENV}" >> "$GITHUB_OUTPUT"
#           echo "ecr_repo=acme-cloud-${ENV}-frontend" >> "$GITHUB_OUTPUT"
#           echo "eks_cluster=acme-cloud-${ENV}-eks" >> "$GITHUB_OUTPUT"
#           echo "deploy_role=arn:aws:iam::338449997393:role/acme-cloud-${ENV}-github-deploy-role" >> "$GITHUB_OUTPUT"
#           echo "ingress_group=acme-cloud-${ENV}" >> "$GITHUB_OUTPUT"

#       - name: Configure AWS credentials via OIDC
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: ${{ steps.resolve.outputs.deploy_role }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Login to Amazon ECR
#         id: ecr-login
#         uses: aws-actions/amazon-ecr-login@v2

#       - name: Build and push image
#         env:
#           ECR_REGISTRY: ${{ steps.ecr-login.outputs.registry }}
#           ECR_REPOSITORY: ${{ steps.resolve.outputs.ecr_repo }}
#           IMAGE_TAG: ${{ github.sha }}
#         run: |
#           docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
#           docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
#           echo "IMAGE_URI=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_ENV

#       - name: Update kubeconfig
#         run: aws eks update-kubeconfig --name ${{ steps.resolve.outputs.eks_cluster }} --region ${{ env.AWS_REGION }}

#       - name: Deploy to EKS
#         run: |
#           sed -i "s|IMAGE_PLACEHOLDER|${IMAGE_URI}|g" k8s/deployment.yaml
#           sed -i "s|INGRESS_GROUP_PLACEHOLDER|${{ steps.resolve.outputs.ingress_group }}|g" k8s/ingress.yaml
#           kubectl apply -f k8s/deployment.yaml
#           kubectl apply -f k8s/service.yaml
#           kubectl apply -f k8s/ingress.yaml
#           kubectl rollout status deployment/frontend-service --timeout=180s